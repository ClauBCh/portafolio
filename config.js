/**
 * Configuración del landing.
 *
 * Contiene los valores de configuración utilizados en el código JavaScript.
 */
export const CONFIG = {
    scrollOffset: 50,
    animationDuration: 1,
    notificationDuration: 3000,
    mobileBreakpoint: 768,
    emailRegex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    recaptchaSiteKey: 'TU_CLAVE_DE_SITIO'
};
